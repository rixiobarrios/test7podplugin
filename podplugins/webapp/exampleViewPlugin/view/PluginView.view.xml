<mvc:View 
    xmlns:l="sap.ui.layout" 
    xmlns:mvc="sap.ui.core.mvc" 
    xmlns="sap.m" 
    controllerName="sapdmc.ext.viewplugins.exampleViewPlugin.controller.PluginView" 
    width="100%" 
    height="100%">
    <Panel id="podModelPluginPanel" width="100%" height="100%" expandable="false" expanded="true" accessibleRole="Region" backgroundDesign="Transparent">
        <headerToolbar>
            <Toolbar height="3rem">
                <Title text="{i18n>title}"/>
                <ToolbarSpacer/>
                <Button id="closeButton" icon="sap-icon://decline" press="onClosePress"/>
            </Toolbar>
        </headerToolbar>
        <content>
            <VBox id="podModelPluginPanelContent" width="100%" height="100%">
                <items>
                    <HBox width="100%">
                        <items>
                            <Label id="podTypeLabel" text="{i18n>podType}:" class="sapUiTinyMarginEnd"/>
                            <Label id="podType" text="{/podType}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%">
                        <items>
                            <Label id="inputTypeLabel" text="{i18n>inputType}:" class="sapUiTinyMarginEnd"/>
                            <Label id="inputType" text="{/inputType}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="workCenterLabel" text="{i18n>workCenter}:" class="sapUiTinyMarginEnd"/>
                            <Label id="workCenter" text="{/workCenter}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="operationLabel" text="{i18n>operation}:" class="sapUiTinyMarginEnd"/>
                            <Label id="operation" text="{/operation}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%">
                        <items>
                            <Label id="resourceLabel" text="{i18n>resource}:" class="sapUiTinyMarginEnd"/>
                            <Label id="resource" text="{/resource}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%">
                        <items>
                            <Label id="selectionCountTypeLabel" text="{i18n>selectionCount}:" class="sapUiTinyMarginEnd"/>
                            <Label id="selectionCount" text="{/selectionCount}" width="100%" />
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="inputLabel" text="{i18n>input}:" class="sapUiTinyMarginEnd"/>
                            <Label id="input" text="{/input}" width="100%" />
                            <Text text="{input}"/>
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="sfcLabel" text="{i18n>sfc}:" class="sapUiTinyMarginEnd"/>
                            <Label id="sfc" text="{/sfc}" width="100%" />
                            <Text text="{sfc}"/>
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="materialLabel" text="{i18n>material}:" class="sapUiTinyMarginEnd"/>
                            <Label id="material" text="{/material}" width="100%" />
                            <Text text="{material}"/>
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="shopOrderLabel" text="{i18n>shopOrder}:" class="sapUiTinyMarginEnd"/>
                            <Label id="shopOrder" text="{/shopOrder}" width="100%" />
                            <Text text="{shopOrder}"/>
                        </items>
                    </HBox>
                    <HBox width="100%" visible="{= ${/podType} === 'WORK_CENTER'}">
                        <items>
                            <Label id="operationCountLabel" text="Operations:" class="sapUiTinyMarginEnd"/>
                            <Label id="operationCount" text="{/operationCount}" width="100%" />
                        </items>
                    </HBox>                                                            
                    <!-- Start Here -->
                    <Table
                        id="selectionsTable"
                        backgroundDesign="Solid"
                        inset="false"
                        width="100%"
                        mode="SingleSelectMaster"
                        items="{
                            path: '/selections'
                        }">
                        
                        <columns> 
                            <Column hAlign="Begin">
                                <Text text="Input"  wrapping="true"/>
                            </Column>
                            <Column hAlign="Begin">
                                <Text text="SFC"  wrapping="true"/>
                            </Column>
                            <Column hAlign="Begin">
                                <Text text="Material" />
                            </Column>
                            <Column hAlign="Begin">
                                <Text text="Shop Order" />
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{input}"/>
                                    <Text text="{sfc}"/>
                                    <Text text="{material}"/>
                                    <Text text="{shopOrder}"/>
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                    <Table
                        id="operationsTable"
                        backgroundDesign="Solid"
                        inset="false"
                        width="100%"
                        mode="SingleSelectMaster"
                        visible="{= ${/podType} === 'WORK_CENTER'}"
                        items="{
                            path: '/operations'
                        }">
                        <columns>
                            <Column hAlign="Begin">
                                <Text text="operation"/>
                            </Column>
                            <Column hAlign="Begin">
                                <Text text="Version"/>
                            </Column>
                        </columns>
                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{operation}"/>
                                    <Text text="{version}"/>
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                    <!-- End Here -->
                </items>
            </VBox>
        </content>
    </Panel>
</mvc:View>